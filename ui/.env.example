# Browser-visible vars must use NEXT_PUBLIC_* (see Next.js env docs).
# For local dev, copy to `.env.local` (see also `.env.local.example`).

# FastAPI base URL — no trailing slash; used by @/lib/api for fetch/SSE.
NEXT_PUBLIC_API_URL=http://localhost:8000
