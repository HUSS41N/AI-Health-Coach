# Copy to .env.local (Next.js loads this for local dev; not committed by default).
# FastAPI base URL — no trailing slash. Browser calls this URL (use localhost, not the Docker service name).
NEXT_PUBLIC_API_URL=http://localhost:8000
